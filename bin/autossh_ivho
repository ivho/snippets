#!/bin/sh
DIR=/home/ivho/var
PIDFILE=$DIR/autossh_datan.pid

[ -d /proc/$(cat $PIDFILE) ] || AUTOSSH_PIDFILE=$PIDFILE autossh datan -R 8888:localhost:8888 -R 11022:localhost:22 -f -N

date >> $DIR/autossh_stamp.log

